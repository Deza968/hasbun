# Configuración inicial de MinIO (bucket por defecto).
# Se crea automáticamente en el primer arranque vía mc si se desea.
# Referencia: http://localhost:9001 (console) credenciales por defecto minioadmin/minioadmin

BUCKET_NAME=hasbun