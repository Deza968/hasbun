"use client";import {useQuery} from "@tanstack/react-query";import {api} from "@/lib/api";export default function Detalle({params}:{params:{id:string}}){const q=useQuery({queryKey:["sale",params.id],queryFn:async()=>(await api.get(`/sales/${params.id}`)).data}); if(q.isLoading) return <p>Cargando</p>; return <div><h1>{q.data?.code}</h1><pre>{JSON.stringify(q.data,null,2)}</pre></div>}
